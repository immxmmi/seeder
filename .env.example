# Input Generator Configuration
# Copy this file to .env and configure your values

# API Tokens (referenced by token_env in settings.yaml)
CMDB_TOKEN=your-cmdb-api-token-here

# TLS Settings
DISABLE_TLS_VERIFY=false
CA_BUNDLE=

# Output Override (optional, overrides settings.yaml)
# OUTPUT_FILE=../acs-provisioner/src/pipelines/inputs.yaml

# Debug Settings
DEBUG_ENABLED=false

# Timeout
API_TIMEOUT=30
